import { BackgroundSlider, HeroContainer, Timers } from "@/components";

const Hero = () => {
  return (
    <div className="hero">
      <Timers />
      <BackgroundSlider />
      <HeroContainer />
    </div>
  );
};
export default Hero;
